import { StyleSheet, Text, TouchableOpacity, View, Alert, Platform, StatusBar, Image, ScrollView } from "react-native";
import styles from '../Estilos.jsx'
export default function Resultado(props) {
    // if(props.res != []){
//     return ()
// }
}